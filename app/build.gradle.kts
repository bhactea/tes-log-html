plugins {
    id("com.android.application")
    kotlin("android")
}

android {
    defaultConfig {
        applicationId = "com.harpa.logger"
        versionCode   = 1
        versionName   = "1.0"
    }
    // … (compileSdk, buildTypes, dll.)
}
